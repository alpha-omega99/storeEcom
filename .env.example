# ============================================================
# ChicShop — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs réelles
# NE JAMAIS commiter .env dans Git !
# ============================================================

# --- Django Core ---
SECRET_KEY=CHANGE_ME_GENERATE_WITH_python_-c_"from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,chicshop.ci,www.chicshop.ci
DJANGO_SETTINGS_MODULE=config.settings.production

# --- Base de données  ---
# Configuration de la base de données
DB_NAME=alpha
DB_USER=ecom_db
DB_PASSWORD=Homme224$0586838902
DB_HOST=127.0.0.1
DB_PORT=3306

# --- Redis (cache + sessions + Celery) ---
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# --- Email SMTP ---
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=bhaalpha4@gmail.com
EMAIL_HOST_PASSWORD=Homme224
DEFAULT_FROM_EMAIL=ChicShop <bhaalpha4@gmail.com>

# --- WhatsApp Business API ---
WHATSAPP_API_TOKEN=YOUR_WHATSAPP_TOKEN
WHATSAPP_PHONE_NUMBER_ID=YOUR_PHONE_ID
WHATSAPP_ADMIN_NUMBER=+2250103976779

# --- Sécurité ---
CORS_ALLOWED_ORIGINS=https://chicshop.ci,https://www.chicshop.ci
CSRF_TRUSTED_ORIGINS=https://chicshop.ci,https://www.chicshop.ci
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# --- Media / Storage ---
MEDIA_ROOT=www/chicshop/media
STATIC_ROOT=www/chicshop/static

# --- Rate Limiting ---
RATELIMIT_USE_CACHE=default

# --- Logs ---
LOG_LEVEL=WARNING
LOG_FILE=django.log
SECURITY_LOG_FILE=security.log